name: Go Build for Multi-Platform

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  push:
    tags: ["v*"]

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.24'

      - name: Build for All Platforms
        run: |
          # 编译 Mac (Intel 芯片)
          GOOS=darwin GOARCH=amd64 go build -o hello-mac-intel
          # 编译 Mac (M1/M2/M3 芯片)
          GOOS=darwin GOARCH=arm64 go build -o hello-mac-apple-silicon
          # 编译 Windows
          GOOS=windows GOARCH=amd64 go build -o hello-windows.exe
          # 编译 Linux
          GOOS=linux GOARCH=amd64 go build -o hello-linux

      - name: Upload to Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            hello-mac-intel
            hello-mac-apple-silicon
            hello-windows.exe
            hello-linux
